name: Verificação de Pull Request

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  # Verificação rápida para PRs
  pr-check:
    name: Verificação de PR
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout do código
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: logoshub-app/package-lock.json
        
    - name: Instalar dependências
      run: |
        cd logoshub-app
        npm ci
        
    - name: Verificar formatação
      run: |
        cd logoshub-app
        npm run format:check
        
    - name: Executar linting
      run: |
        cd logoshub-app
        npm run lint
        
    - name: Executar testes unitários
      run: |
        cd logoshub-app
        npm run test:ci
        
    - name: Build de verificação
      run: |
        cd logoshub-app
        npm run build:prod
        
    - name: Verificar vulnerabilidades
      run: |
        cd logoshub-app
        npm audit --audit-level=high
        
    - name: Comentar no PR
      uses: actions/github-script@v7
      with:
        script: |
          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
          });
          
          const botComment = comments.find(comment => 
            comment.user.type === 'Bot' && 
            comment.body.includes('✅ Verificação de PR concluída')
          );
          
          const message = `✅ **Verificação de PR concluída com sucesso!**
          
          - ✅ Formatação do código verificada
          - ✅ Linting executado sem erros
          - ✅ Testes unitários passaram
          - ✅ Build de produção bem-sucedido
          - ✅ Verificação de segurança concluída
          
          🚀 **Pronto para merge!**`;
          
          if (botComment) {
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: botComment.id,
              body: message
            });
          } else {
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: message
            });
          }
